<UserControl x:Class="dronePath.PahtDraw"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Canvas Name="grd_path" MouseLeftButtonDown="Grd_path_MouseLeftButtonDown"  Height="720" Width="1280" Background="AliceBlue">
        <Button Panel.ZIndex="100" Click="Button_Click">Terminé</Button>
        <Button Panel.ZIndex="100" Name="sleep" Click="sleep_Click" Canvas.Left="51">Sleep (500 ms)</Button>
        <StackPanel>
            <Label Name="lbl_type"></Label>
            <Label Name="lbl_id"></Label>
        </StackPanel>
    </Canvas>
</UserControl>
